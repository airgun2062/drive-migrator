[package]
name = "cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[[bin]]
name = "migrator"
path = "src/main.rs"

[dependencies]
engine = { path = "../engine" }
clap = { version = "4", features = ["derive"] }
serde_json = "1"
