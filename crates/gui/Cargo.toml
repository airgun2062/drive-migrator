[package]
name = "gui"
version = "0.1.0"
description = "Drive Migrator GUI"
edition.workspace = true
license.workspace = true

[lib]
name = "gui_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = "2"

[dependencies]
engine = { path = "../engine" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
log = "0.4"
tauri = "2"
tauri-plugin-log = "2"
tauri-plugin-dialog = "2"
